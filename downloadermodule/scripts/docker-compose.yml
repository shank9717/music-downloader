version: "3.2"
services:
    music-downloader-service:
        container_name: music-downloader-service
        image: 'shashankullas/music-downloader:2.1.0'
        restart: always
        env_file:
            - .env
        ports:
          - 8000:8000